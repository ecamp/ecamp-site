---
import Item from "~/components/blog/ListItem.astro";

const { posts } = Astro.props;
---

<ul>
  {
    posts.map(({props}, index) => (
      <li class="mb-10 md:mb-16">
        <Item {...props} first={index === 0} />
      </li>
    ))
  }
</ul>
